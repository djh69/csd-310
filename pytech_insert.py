MongoDB: insert_one()  
dillon = {
 “first_name”: “Dillon”
}
 
dillon_student_id = students.insert_one(dillon).inserted_id
 
print(dillon_student_id)

MongoDB: insert_one() 
taylor = {
 “first_name”: “Taylor”
}
 
taylor_student_id = students.insert_one(taylor).inserted_id
 
print(taylor_student_id)

MongoDB: insert_one() 
lexi = {
 “first_name”: “Lexi”
}
 
fred_student_id = students.insert_one(Lexi).inserted_id
 
print(Lexi_student_id)